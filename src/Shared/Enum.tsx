export enum StogareItemEnum {
  File = 0,
  Folder = 1,
  DefaultMainFolder = -1,
}

export enum AlertType {
  Default,
  Warning,
  Error,
}
export enum StatusNote {
  Default = 0,
  Archive = 1,
  Remove = 2,
}
